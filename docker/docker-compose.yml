version: "3.9"
services:
  api:
    build:
      context: ..
      dockerfile: Dockerfile
    env_file:
      - ../.env
    volumes:
      - ../ffz.db:/app/ffz.db  # persiste la DB sqlite
    ports:
      - "8000:8000"
    restart: unless-stopped
